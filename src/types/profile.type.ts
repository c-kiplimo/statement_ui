type CustomerProfile ={
    profileId?: string,
    profileName: string,
    country?: string,
    userId: string,
    customerId?: number,
    active?: boolean,
    defaultProfile?: boolean,
    name?:string
}