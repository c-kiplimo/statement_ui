import { ReactNode } from "react";

const getaccounts = (param: {
  hideicon: ReactNode;
  editicon?: ReactNode;
  creationDate: string;
  acctNumber: string;
  numberofTimes: string;
  fileformart?: string;
  acctStatus: string;
  date: string;
  status: string;
  custName: string;
  duration: string;
  filetype?: string;
  time?: string;
  acctbal?: string;
  timefrequency?: string;
  default?: string;
}) => {

};
